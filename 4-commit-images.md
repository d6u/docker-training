# `commit`, `images`

1. `docker commit`

    ```
    docker commit <container-id> daiwei/awesome:v1
    ```

2. Use "image"

    ```
    docker run -i -t daiwei/awesome:v1 bash
    cat /hellofile
    ```
